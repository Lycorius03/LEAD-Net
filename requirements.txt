# requirements.txt — 本地 torchenv 实测锁定
# 依据 docs/ARCHITECTURE.md §跨平台适配规范 3（精确到版本号，尤其 torch/torchvision）。
#
# 本地环境（environment/local.md）：Windows 11 / Python 3.11.15 / RTX 5060 Blackwell / CUDA 12.8。
# 云端环境（environment/cloud.md）：AutoDL，CUDA / torch 版本待 cloud.md 确认后另出 requirements-cloud.txt。
#
# torch / torchvision 的 CUDA 构建安装方式（本地 cu128，须用 PyTorch 官方 index）：
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
# 或直接用本文件配合该 index 安装：
#   pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu128 --extra-index-url https://pypi.org/simple
# （下方 torch/torchvision 行的 +cu128 为本地版本标签，需走上述 index 才能解析。）

# --- 核心：深度学习框架（精确锁定）---
torch==2.11.0+cu128          # Blackwell 需要 >= 2.7.0
torchvision==0.26.0+cu128
torchmetrics==1.9.0

# --- 数值 / 图像 / COCO 数据管道 ---
numpy==2.4.4
pillow==12.2.0
opencv-python==4.13.0.92
pycocotools==2.0.11

# --- 配置 / 日志 / 实验数据 ---
pyyaml==6.0.3
matplotlib==3.10.9
pandas==3.0.3

# --- 测试（可选）---
# tests/test_imports.py 既可用 pytest 也可直接 `python tests/test_imports.py` 运行；
# 若需 pytest 解析断言再加包：
# pytest==8.4.2